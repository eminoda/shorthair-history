import { DeformationDirective } from './deformation.directive';

describe('DeformationDirective', () => {
  it('should create an instance', () => {
    const directive = new DeformationDirective();
    expect(directive).toBeTruthy();
  });
});
